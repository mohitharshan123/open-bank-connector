{
  "name": "open-bank-sdk",
  "version": "1.0.0",
  "description": "Open bank integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && echo 'Build completed - dist folder created'",
    "prepublishOnly": "npm run build",
    "postinstall": "npm run build",
    "dev": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}